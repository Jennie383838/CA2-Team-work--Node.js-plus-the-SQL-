<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<title>Edit Event Details</title>
</head>
<body>
<!-- Navbar -->
<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
            <a class="navbar-brand" href="/">RP HUB App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
            </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/register">Register</a>
            </li> 
            <li class="nav-item">
                <a class="nav-link" href="/login">Login</a>
            </li> 
            </ul>
        </div>
    </div>
</nav>

    <!-- Main content -->
    <div class="container mt-4">
    <h2 class="text-center mb-4">Edit Event Details</h2>
    
    <% if (event) { %>
    <form action="/editEvent/<%= event.id %>" method="POST" enctype="multipart/form-data" class="mx-auto" style="max-width: 500px;">
        
        <div class="mb-3">
                <label for="eventName">Event Name:</label>
                <input type="text" class="form-control" id="eventName" name="eventName" value="<%= event.name %>" required>
            </div>

            <div class="mb-3">
                <label for="type">Event Type:</label>
                <input type="text" class="form-control" id="type" name="type" value="<%= event.type %>" required>
            </div>

            <div class="mb-3">
                <label for="subject">Event Subject:</label>
                <input type="text" class="form-control" id="subject" name="subject" value="<%= event.subject %>" required>
            </div>

            <div class="mb-3">
                <label for="description">Event Description:</label>
                <input type="text" class="form-control" id="description" name="description"value="<%= event.event_description %>"  required>
            </div>

            <div class="mb-3">
                <label for="location">Location:</label>
                <input type="text" class="form-control" id="location" name="location" value="<%= event.location %>"required>
            </div>

            <div class="mb-3">
                <label for="budget">Event Budget:</label>
                <input type="float" class="form-control" id="budget" name="budget" value="<%= event.budget %>"required>
            </div>

            <div class="mb-3">
                <label for="email">Contact Email:</label>
                <input type="email" class="form-control" id="email" name="email" value="<%= event.email %>"required>
            </div>
    </form>
    <% } else { %>
        <p class="text-center">No product found.</p>
    <% } %>
</div>
</body>
</html>
